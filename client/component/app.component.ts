import {Component} from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'my-app',
  template: `
    <a href="#">abc</a>
  `
})
export class AppComponent {
  title = 'Tours of heroes';
}